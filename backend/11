import axios from 'axios';

const API_BASE = import.meta.env.VITE_API_BASE || 'http://localhost:5000/api';

export async function generateAI({ platform, prompt, length='short' }) {
  const resp = await axios.post(`${API_BASE}/generate`, { platform, prompt, length }, { timeout: 60000 });
  return resp.data;
}
